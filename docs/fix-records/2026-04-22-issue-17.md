# Issue #17 修复记录

> 修复日期：2026-04-22
> 修复人：Claude Code

---

## Issue #17：playwright.config.ts 有重复字段和错误的 baseURL

### 问题描述

`config/playwright.config.ts` 有两个问题：

**1. `outputFolder` 字段重复定义：**
```typescript
// 修复前（重复定义）
reporter: [
  ['allure-playwright', {
    outputFolder: 'allure-results',   // 第一次
    suiteTitle: false,
    detail: true,
    outputFolder: './allure-results'  // 第二次，覆盖了第一次
  }]
],
```

**2. `baseURL` 硬编码为 `localhost:3000`，但测试的是百度等外部网站：**
```typescript
// 修复前
use: {
  baseURL: 'http://localhost:3000',  // 测试百度时完全用不到
}
```

### 修复方案

```typescript
// 修复后
reporter: [
  ['allure-playwright', {
    outputFolder: './allure-results',  // 只保留一个，用相对路径
    suiteTitle: false,
    detail: true,
  }]
],
use: {
  baseURL: process.env.TEST_BASE_URL || 'https://example.com',  // 从环境变量读取
}
```

### 修改文件

| 文件 | 修改内容 |
|------|----------|
| `config/playwright.config.ts` | 移除重复的 outputFolder，baseURL 改为环境变量 |

### 主要改动

1. **移除重复的 `outputFolder`** — 只保留一个，使用相对路径 `./allure-results`
2. **baseURL 改为环境变量** — 通过 `TEST_BASE_URL` 环境变量配置，默认值改为 `https://example.com`

---

## 总结

| Issue | 标题 | 状态 | 单元测试 |
|-------|------|------|----------|
| #17 | playwright.config.ts 重复字段和错误 baseURL | ✅ 已修复 | 配置文件修复，无需单元测试 |
